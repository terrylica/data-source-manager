---
description: 
globs: 
alwaysApply: false
---
# Single Source of Truth Configurations

- `utils/config.py` is the single source of truth of for parameters
- `utils/market_constraints.py` is single source of truth for data types
- interval eum in the `utils/market_constraints.py` must be used as the only interval in all codes.
