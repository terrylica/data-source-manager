# Local mise configuration (copy to .mise.local.toml)
# This file contains secrets and should NOT be committed to git
#
# Setup:
#   1. Copy this file: cp .mise.local.toml.example .mise.local.toml
#   2. Create GitHub token at: https://github.com/settings/tokens
#      Required scopes: repo, workflow
#   3. Store token: echo "ghp_YOUR_TOKEN" > ~/.claude/.secrets/gh-token-yourname
#   4. Update path below to your token file

[env]
GH_TOKEN = "{{ read_file(path=env.HOME ~ '/.claude/.secrets/gh-token-yourname') | trim }}"
GITHUB_TOKEN = "{{ read_file(path=env.HOME ~ '/.claude/.secrets/gh-token-yourname') | trim }}"
